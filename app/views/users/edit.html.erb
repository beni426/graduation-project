<h1>プロフィール編集</h1>

<%= form_with model: @user do |form| %>
  <div>name<%= form.text_field :name %></div>
  <div>プロファイル写真<%= form.file_field :image %></div>
  <div><%= form.submit "保存" %></div>
<% end %>
<%= link_to 'Back', user_path(current_user) %>